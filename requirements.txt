# Core modeling (matches notebook: XGBoost + sklearn preprocessing)
pandas>=2.0.0,<3
numpy>=1.24.0,<3
scikit-learn>=1.3.0,<2
joblib>=1.3.0,<2

# Optional: pip install xgboost and use `--backend xgboost` (macOS OpenMP often needs `brew install libomp`).

# HTTP API for real-world consumption
fastapi>=0.110.0,<1
uvicorn[standard]>=0.29.0,<1
pydantic>=2.0.0,<3
